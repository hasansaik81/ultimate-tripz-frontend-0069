import { Button } from "@nextui-org/button";
import { FiEdit3 } from "react-icons/fi";

const EditPost = () => {
  return (
    <div>
      <Button onClick={() => {}} isIconOnly className="">
        <FiEdit3 className="text-lg" />
      </Button>
    </div>
  );
};

export default EditPost;
